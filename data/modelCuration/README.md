# Model Curation Data

Store TSV or CSV files here when they support one round of scripted model
curation. Use versioned subfolders, such as `v1_0_1/`, when several files
belong to the same curation script.

Do not store generated binary model outputs here.
