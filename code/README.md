# code

This folder contains the scripts used to generate, analyse and manipulate the eciYali5-GEM models and related outputs. It is organised to separate project-specific analysis and generation scripts from reusable helper utilities.

## Top-level layout (current repository)

- README.md — this file
- debiaggi2025/ — main analysis and model-generation scripts (MATLAB)
- utilities/ — reusable helper functions and small utilities

### debiaggi2025/
This subfolder contains the core model-generation and analysis scripts used by the Debiaggi 2025 work. Key files and subfolders currently present:

- analysisFSEOF.m
  - Flux Scanning based on Enforced Objective Function (FSEOF) analysis script.
- analyzeEcGEMs.m
  - Scripts to compare / analyse enzyme-constrained GEM variants.
- eciYali5GEMadapter.m
  - Adapter / wrapper to convert or adapt model structures to the eciYali5GEM conventions.
- fluxControlCoefficients.m
  - Compute flux control coefficients 
- generateProteomeModels.m
  - Generate proteome-constrained model variants or proteome-model inputs.
- generate_eciYali5GEM.m
  - Script to build the eciYali5GEM model
- generate_iYali5GEM.m
  - Script to build the iYali5GEM model
- R_Analysis/ (directory)
  - R scripts and analyses related to model results or downstream statistics.
- output/ (directory)
  - Output files produced by the scripts above (models, figures, intermediate results).

### utilities/
This folder contains reusable MATLAB helper functions for model construction, biomass handling, lipid pool updates, FSEOF analysis and export utilities used by the eciYali5-GEM project. Functions assume a model structure compatible with common COBRA/GECKO formats and often rely on a ModelAdapter object (ModelAdapterManager.getDefault()) to locate repository paths and parameters.

## Typical usage
- Open MATLAB in the repository root (or add the repository to the MATLAB path).
- Run a generator script to build a model, for example:
  - >> run('code/debiaggi2025/generate_eciYali5GEM.m')
  - or call the main function with parameters as described in its file header.
- Analysis scripts usually assume a model is available (either loaded from `.mat` or generated by the generator scripts).
- R analyses are stored under `debiaggi2025/R_Analysis/`; run them with R (or RStudio) as appropriate.

Always check the top of each script for required inputs, optional parameters and expected outputs.

## Dependencies
Specify exact versions in repository-level documentation where possible. Typical dependencies include:
- MATLAB (version compatible with the scripts)
- Modeling toolboxes (COBRA Toolbox / RAVEN / other toolboxes as required by the code)
- R and relevant R packages (for scripts in `R_Analysis/`)

## Where to look next
- Inspect the header comments of the scripts in `code/debiaggi2025/` for per-script instructions.
- Check `code/utilities/` for shared helpers that may be useful when writing new scripts.